return {
  "nvimdev/dashboard-nvim",
  opts = {
    config = {
      -- header = vim.split(string.rep("\n", 8) .. "Robert Editor" .. "\n\n", "\n"),
      header = vim.split(string.rep("\n", 8) .. "" .. "\n\n", "\n"),
    },
  },
}
